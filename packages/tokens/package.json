{
  "name": "@repo/tokens",
  "version": "0.0.1",
  "main": "index.js",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && node config.mjs",
    "clean": "rimraf dist && rimraf node_modules"
  },
  "devDependencies": {
    "style-dictionary": "^4.0.0-prerelease.30"
  }
}
